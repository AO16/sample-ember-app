<div class='container'>
  <h2>Users</h2>
  <div class='row'>
    <div class='col-xs-12'>
      <ul>
        {{#each model as |user|}}
          <li>{{link-to user.name 'home.user' user.id}}</li>
        {{/each}}
      </ul>
    </div>
  </div>
</div>
{{outlet}}
